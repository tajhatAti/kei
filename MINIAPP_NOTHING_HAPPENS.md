# আসল বাগ পাওয়া গেছে — এবং সেটা আমাদের কোডে

তারিখ: 2026-08-16 · ব্রাঞ্চ: `arena/01a009d6-claude`

---

## সত্যিটা

> **আপনার টোকেন, আপনার বট, আপনার কনফিগ — সব ঠিক ছিল।**
>
> বাগটা `services/miniapp_auth.py`-তে ছিল: HMAC হিসাব করার সময় আমরা
> `signature` ফিল্ডটা **বাদ দিয়ে দিচ্ছিলাম**, অথচ Telegram ওটাকে
> **হ্যাশের ভেতরে রেখে** সাইন করে।
>
> এক লাইনের ভুল। চার রাউন্ড ধরে আমি টোকেনকে দোষ দিয়েছি।

আপনি নতুন বট বানিয়েছেন, টোকেন বদলেছেন, সঠিক বট খুলেছেন — **কোনোটাই কাজ করত
না**, কারণ সমস্যা ওখানে ছিলই না। এর জন্য আন্তরিকভাবে দুঃখিত।

---

## ভুল কোডটা

```python
received_hash = pairs.pop("hash", "")
pairs.pop("signature", None)      # ← এই লাইনটাই বাগ
data_check = "\n".join(f"{k}={pairs[k]}" for k in sorted(pairs))
```

কমেন্টে লেখা ছিল "signature হলো third-party validation-এর ফিল্ড, HMAC
data-check string-এ থাকে না"। এটা **Ed25519-এর জন্য সত্য, HMAC-এর জন্য মিথ্যা**।

Telegram-এর নিয়ম আসলে দুটো, আর সেগুলো আলাদা:

| যাচাই | যা বাদ দিতে হয় |
|---|---|
| **HMAC** (বট টোকেন দিয়ে) | শুধু `hash` |
| **Ed25519** (third-party) | `hash` **এবং** `signature` |

দুটো নিয়ম গুলিয়ে ফেলাই ছিল ভুল।

### প্রমাণ — Telegram-এর নিজের লাইব্রেরি দিয়ে

`@telegram-apps/init-data-node` দিয়ে `signature`-সহ একটা পেলোড সাইন করে
দুইভাবে হ্যাশ মিলিয়ে দেখেছি:

```
হ্যাশ (লাইব্রেরির)         : bb7b679dc00775c3...
HMAC signature সহ          : bb7b679dc00775c3...  ← মিলে গেছে ✅
HMAC signature ছাড়া        : 981c1132292cc3d0...  ← মেলেনি ❌
```

অর্থাৎ ফিল্ডটা **হ্যাশের অংশ**। বাদ দিলে বাইট বদলে যায় → `bad_hash`।

---

## চার রাউন্ড কেন লাগল — তিনটে ফাঁক

`pop` লাইনটা **ডায়াগনস্টিক তৈরির আগেই** চলত, তাই:

1. **লগে `signature` দেখাই যেত না** — তাই প্রতিবার
   `fields=['auth_date','query_id','user']` — একদম স্বাভাবিক তালিকা, কিছু বাদ
   নেই, বাড়তি নেই
2. **culprit finder ধরতে পারেনি** — সেটা শুধু ফিল্ডের *মান* বদলে দেখে, **বাদ
   পড়া ফিল্ড** ধরতে পারে না → `culprit=None`
3. **Ed25519 চেক `VALID` বলছিল** (সেটা ঠিকই ছিল, ওখানে বাদ দেওয়াই নিয়ম) আর
   getMe `ok` বলছিল — **দুটো সবুজ বাতি পেলোড থেকে দৃষ্টি সরিয়ে দিচ্ছিল**

সার্ভার যা দেখাচ্ছিল: "পেলোড আসল, টোকেন জীবিত, ফিল্ড স্বাভাবিক" — ঠিক এই
প্রমাণগুলোই আমাকে টোকেনের দিকে ঠেলেছে।

### আর একটা টেস্ট বাগটাকে পাহারা দিচ্ছিল

`test_miniapp_auth`-এ একটা টেস্ট ছিল যেটা signature **ছাড়া** সাইন করা পেলোডে
পরে `signature=abc123` **জুড়ে দিয়ে** দাবি করত সেটা পাস করবে। Telegram এমন
স্ট্রিং কখনো বানায় না। ওই টেস্ট পাস করার **একমাত্র উপায়** ছিল `signature`
pop করা — অর্থাৎ **টেস্টটাই বাগটা ধরে রেখেছিল**।

> যে টেস্ট পাস করতে হলে প্রোডাকশন ভাঙতে হয়, সেটা টেস্ট না থাকার চেয়েও খারাপ।

---

## ✅ যা করবেন

**শুধু ডিপ্লয় করুন।** কোনো টোকেন বদলাবেন না, নতুন বট বানাবেন না।

ডিপ্লয়ের পর Mini App খুললেই সাইন-ইন হয়ে যাবে।

---

## যা বদলেছে

| ফাইল | পরিবর্তন |
|---|---|
| `services/miniapp_auth.py` | **`pop("signature")` সরানো** — আসল ফিক্স। Ed25519 পথে ওটা থাকছে (সেখানে নিয়মই তাই) |
| `services/miniapp_auth.py` | লগে এখন **যা এসেছে** সেই ফিল্ড তালিকা (pop-এর পরের নয়) |
| `services/miniapp_auth.py` | নতুন প্রোব: কোনো ফিল্ড **বাদ দিলে/যোগ করলে** হ্যাশ মেলে কিনা — মিললে লগে লেখে **"bug in the verifier, not a bad token"** |
| `tests/test_signature_field.py` | **নতুন** — ১৩টি টেস্ট |
| `tests/test_miniapp_auth.py` | বাগ-পাহারা দেওয়া টেস্টটা আসল চুক্তি অনুযায়ী লেখা |

### নিরাপত্তা যাচাই (টেস্টে)
- `signature`-সহ আসল পেলোড → ✅ পাস
- signature **পরে জুড়ে দেওয়া** পেলোড → ❌ প্রত্যাখ্যাত
- অন্য বটের টোকেন → ❌ প্রত্যাখ্যাত
- tampered পেলোড → ❌ প্রত্যাখ্যাত
- Telegram-এর অফিসিয়াল টেস্ট ভেক্টর (signature ছাড়া) → ✅ পাস

## টেস্ট ফলাফল

```
test_signature_field   13 passed, 0 failed   (নতুন)
test_token_mismatch    20 passed, 0 failed
test_badhash_evidence  23 passed, 0 failed
test_badhash_verdict   24 passed, 0 failed
test_miniapp_opens     33 passed, 0 failed
test_all_routes        65 pass,   0 fail
test_web_batch         24 pass,   0 fail
test_security_batch    22 pass,   0 fail
test_miniapp_auth     128 passed, 3 failed   (আগে ৩ ফেল — বেসলাইন, /code সংক্রান্ত)
test_telegram_link     78 passed, 1 failed   (বেসলাইন)
test_bot_critical      32 pass,   4 fail     (বেসলাইন)
```

---

## নিরপেক্ষভাবে: আমার চারটে ভুল

| রাউন্ড | আমি বলেছিলাম | আসলে |
|---|---|---|
| ১ | "টোকেন পুরনো" | শর্তটা tautology ছিল |
| ২ | "পুরনো বটের বাটন" | `query_id` কোন বট সাইন করেছে বলে না |
| ৩ | "Revoke করুন" | টোকেন বৈধ ছিল |
| ৪ | "ডিপ্লয়ে ভুল মান" | **কনফিগে কিছুই ভুল ছিল না — কোডে ছিল** |

প্রতিবার আমি সার্ভারের নিজের সংকেত বিশ্বাস করেছি, অথচ সেই সংকেতগুলোই বাগটার
কারণে বিকৃত ছিল (`signature` লগেই আসত না)। **আপনার `token_live=True` লাইনটাই
আমাকে বাধ্য করেছে নিজের যাচাইকারীকে সন্দেহ করতে** — কারণ "বৈধ টোকেন" আর
"আসল পেলোড" একসাথে সত্য হলে বাকি থাকে শুধু আমাদের নিজেদের হিসাব।
