# UI অডিট ও সংস্কার — কী পাওয়া গেল, কী ঠিক করা হলো

তারিখ: 2026-08-16 · ব্রাঞ্চ: `arena/01a009d6-claude`

---

## পদ্ধতি: চোখে দেখা নয়, মেপে দেখা

স্যান্ডবক্সে হেডলেস ব্রাউজার ইনস্টল করা যায়নি, তাই স্ক্রিনশট নেওয়া সম্ভব ছিল
না। তার বদলে আসল পেজটা jsdom-এ বুট করে **প্রতিটা কন্ট্রোলের computed style
মেপে** দেখা হয়েছে, তিনটা ভিন্ন স্ক্রিন সাইজে (desktop / 760px / 390px)।

একটা গুরুত্বপূর্ণ কারিগরি বিষয়: jsdom `var(--ctl-h-sm)` হুবহু ফেরত দেয়, মান
নয়। তাই প্রথম দিকের মাপগুলো ভুল ছিল — `parseFloat` করলে `NaN` আসে, আর `NaN`
দিয়ে তুলনা **সবসময় false**, অর্থাৎ টেস্ট নীরবে অর্থহীন হয়ে যায়। এজন্য
`tests/js/cssvars.js` লিখতে হয়েছে যা cascade ধরে টোকেন resolve করে।

---

## যা পাওয়া গেল ও ঠিক করা হলো

### ১. এক কাজের জন্য সাতটা উচ্চতা

মাপা ফল:

| কন্ট্রোল | উচ্চতা ছিল |
|---|---|
| বাটন | 40px |
| টেক্সট ইনপুট | **42px** |
| সিলেক্ট | 32px |
| `.rs-inp` | 34px |
| `.rs-seg-btn` | 28px |
| `.jd-btn` | 34px |
| `.rs-sq` | 30px |

Code Studio হেডার, RunSpace টুলবার আর job-detail বার — তিনটাতেই বাটন আর ফিল্ড
পাশাপাশি বসে। ফলে **সারিগুলো অসমান** দেখাত: ৪০px বাটন তার পাশের ৪২px ফিল্ডের
চেয়ে ২px ছোট, আর অন্য পাশের সিলেক্টের চেয়ে ৮px বড়।

**সমাধান:** তিনটে সাইজ (`--ctl-h-sm` 32 / `--ctl-h` 40 / `--ctl-h-lg` 48), আর
বাটন-ইনপুট-সিলেক্ট **সবাই একই টোকেন পড়ে**। ঘন টুলবার একবার পুরো সারির জন্য
ছোট সাইজ বেছে নেয়, প্রতিটা কন্ট্রোল আলাদা করে নয়।

> লুকানো কারণ: বেস বাটন রুলে `height: 40px` হার্ডকোড ছিল, যা টুলবারের সেট করা
> `--ctl-h`-কে নীরবে হারিয়ে দিত। এজন্যই টোকেন "কাজ করছে না" মনে হচ্ছিল।

### ২. আঠারোটা ফন্ট সাইজ, চারটা অর্ধ-পিক্সেল

`10, 10.5, 11.5, 12, 12.5, 13, 13.5, 14, 15, 16, 17, 18, 20, 21, 24, 26, 27, 30`

১০.৫ বনাম ১১, ১২.৫ বনাম ১৩ — পার্থক্য এত কম যে ইচ্ছাকৃত মনে হয় না, অথচ যথেষ্ট
যে দুটো একই ধরনের লেবেল আলাদা দেখায়। আর অর্ধ-পিক্সেল টেক্সট পিক্সেল গ্রিডে
বসে না, তাই **সামান্য ঝাপসা** দেখায়।

**সমাধান:** আটটা ধাপ, সব পূর্ণ পিক্সেল। ৯৩টা ডিক্লারেশন নিকটতম ধাপে আনা।

### ৩. ফন্টের ওজন যা আদৌ ডাউনলোড হয় না ⚠️

`index.html` Inter লোড করে `400;500;600;700;800`, কিন্তু CSS চাইত
**550, 570, 650, 680, 750, 760, 780**-ও।

ব্রাউজার যে face ডাউনলোড করেনি সেটা রেন্ডার করতে পারে না — নীরবে নিকটতমটায়
গোল করে। ফলে ৫৫০ দেখাত ৫০০ হিসেবে, ৫৭০ দেখাত ৬০০ হিসেবে। **সূক্ষ্ম নিয়ন্ত্রণ
মনে হতো, আসলে কিছুই ছিল না** — আর যে দুটো এলিমেন্ট আলাদা হওয়ার কথা (৬০০ vs
৫৭০) সেগুলো দুর্ঘটনাক্রমে এক দেখাত।

### ৪. ফোনে ঘন কন্ট্রোল ৪৪px নিয়মের বাইরে

মোবাইল রুল শুধু সাধারণ এলিমেন্টের নাম বলত, তাই **প্রতিটা RunSpace কন্ট্রোল
ফোনেও ৩২px** থেকে যেত — নির্দেশিকার এক-তৃতীয়াংশ কম, আর সেটাও অ্যাপের সবচেয়ে
ঘিঞ্জি টুলবারে যেখানে ভুল ট্যাপে ভুল কাজ হয়।

### ৫. পড়া কঠিন ম্লান টেক্সট

`--fg-4` (#5c5c5c) দুটো কাজ করত: "disabled" (যেখানে কম কনট্রাস্টই উদ্দেশ্য)
আর সাধারণ ম্লান টেক্সট — placeholder, log timestamp। মাপা কনট্রাস্ট **2.98:1**,
যা large-text-এর ৩:১ সীমার নিচেও।

**সমাধান:** ভাগ করা — `--fg-4` (#808080, **5.04:1**) পড়ার জন্য, `--fg-off`
সত্যিকারের নিষ্ক্রিয় অবস্থার জন্য।

### ৬. ২৯টা এলোমেলো z-index, দুটো ডুপ্লিকেট

৫ থেকে ১০০০০ পর্যন্ত, আর `.toast-container` ও `.ah-modal` **দু'বার করে ভিন্ন
মানে** — কোনটা জিতবে তা ফাইলের ক্রমের উপর নির্ভর করত, যা সিদ্ধান্ত নয়, দুর্ঘটনা।

**সমাধান:** নামযুক্ত ladder (`--z-sticky` … `--z-fatal`)। এটা করতে গিয়েই একটা
**আসল বাগ** ধরা পড়ল: RunSpace-এর sheet activity panel-এর **নিচে** খুলত।

### ৭. RunSpace-এ bottom nav এডিটরের উপর ভাসত

Details পেজ ওটা লুকাত, কিন্তু RunSpace — যেটা সমানভাবে full-screen — লুকাত না।
ফলে কোড এরিয়ার শেষ ~৬৪px ঢাকা পড়ত।

### ৮. থিম টগল বাটন যা কিছুই করত না ⚠️

দুটো বাটন (ড্যাশবোর্ড + job detail) `data-theme` সেট করত আর নিজের আইকন
বদলাত — **আর কিছুই হতো না**। `app.css` নিজেই বলে *"One product, one theme"*
এবং দুটো মানকেই একই dark palette-এ ম্যাপ করে।

প্রমাণ (দুই থিমে টোকেন resolve করে):
```
dark    --bg #090909  --fg #fcfcfc  --card #161616
light   --bg #090909  --fg #fcfcfc  --card #161616
```

ইউজার চাপে, আইকন বদলায়, স্ক্রিন একই থাকে → **অ্যাপ ভাঙা মনে হয়**। বাটন না
থাকার চেয়েও খারাপ। দুটোই সরানো হয়েছে, কবে ফিরবে সেই শর্ত লিখে।

### ৯. আরও

- **OTP বক্স**: স্ক্রিন রিডারে ছয়টা একই রকম নামহীন ফিল্ড → এখন প্রতিটা ডিজিটে
  আলাদা লেবেল + `autocomplete="one-time-code"` (OS নিজে কোড বসাতে পারবে)
- **Safe area**: `top:0/bottom:0` সাইড প্যানেলের শেষ সারি iPhone-এর home
  indicator-এর নিচে চাপা পড়ত
- **Touch hygiene**: tap-highlight ফ্ল্যাশ, double-tap-এ টেক্সট সিলেক্ট, আর
  scroll chaining (প্যানেলের শেষে পৌঁছালে পেছনের পেজ নড়ত) — তিনটাই বন্ধ
- **4px গ্রিড**: padding/gap-এ ২,৫,৬,৭,৯,১০,১১,১৩,১৪px ছড়ানো ছিল
- **টোকেন ডুপ্লিকেট**: `--accent`, `--nav-height`, `--jd-accent` দুই জায়গায়
  ভিন্ন মানে — আজ একই ফল দিত, কিন্তু একটা বদলালে অন্যটা নীরবে পুরনো থাকত

---

## নতুন টেস্ট

| ফাইল | কী করে |
|---|---|
| `test_ui_consistency.js` | **৪৯ চেক** — উপরের প্রতিটা বিষয় |
| `test_rows_are_level.js` | **৩৬ চেক** — আসল টুলবারের প্রতিটা সারি তিনটা সাইজে সমান কিনা |
| `cssvars.js` | CSS টোকেন resolver (nested fallback সহ) |
| `zlayers.js` | z-index টোকেন resolver |

**যাচাই করা যে টেস্টগুলো অর্থপূর্ণ:** পুরনো CSS-এ চালিয়ে দেখা হয়েছে —
`test_rows_are_level` সেখানে **৬টা fail** দেয় (sign-in form 40/42px, Code
Studio header 32/40px), নতুনে ০।

## ফলাফল

```
৩৯টি JS suite: ৩৬টি সম্পূর্ণ সবুজ
Python suite : সব সবুজ (test_all_routes 65, test_web_batch 24,
               test_security_batch 22, test_signature_field 13)

সারি সমান কিনা: 18 rows × 3 viewports → 0 ragged
```

### নিরপেক্ষভাবে: যা ঠিক করিনি

তিনটে suite-এ ফেল আছে, **তিনটাই আমার কাজের আগে থেকেই** এবং UI-সংক্রান্ত নয়:

- `test_miniapp_ui` (৫) — jsdom ইনলাইন স্টাইলের উপর `!important` মানে না;
  আসল ব্রাউজারে সমস্যা নেই
- `test_telegram_link_ui` (৩) — বট deep-link লজিক
- `test_runspace_single_header` (১) — একটা self-check মেট্রিক

আর কয়েকবার **আমার নিজের টেস্ট/টুলই ভুল ছিল** এবং পুরনো টেস্ট আমাকে ধরিয়ে
দিয়েছে — যেমন `--jd-accent` আমি `--acc`-এ পয়েন্ট করেছিলাম, কিন্তু `--accent`-ই
সঠিক (সেটাই theme-aware)। সেগুলো সংশোধন করা হয়েছে।
