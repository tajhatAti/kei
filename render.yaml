services:
  - type: web
    name: easysell-bd
    runtime: python
    plan: free
    buildCommand: ""
    startCommand: python3 -m http.server $PORT --bind 0.0.0.0 --directory site
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.2"
